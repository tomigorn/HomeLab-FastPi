COMPOSE_PROJECT_NAME=loyalty-cards
IMAGE_TAG=
# The logo.dev token is baked into the image at BUILD time (not used at runtime here).
# Build the image with:
#   docker build --build-arg VITE_LOGODEV_TOKEN=<pk_token> -t tomigorn/loyalty-cards:<tag> .
