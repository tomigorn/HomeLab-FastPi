# Home Assistant secrets — referenced in YAML as: some_key: !secret some_key
# Learn more: https://www.home-assistant.io/docs/configuration/secrets/
#
# Copy this file to secrets.yaml and fill in real values.
# secrets.yaml is gitignored; this .example file is committed as a template.

# Example (delete once you add real ones):
some_password:
