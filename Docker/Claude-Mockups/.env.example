COMPOSE_PROJECT_NAME=claude-mockups

# Basic-auth credentials for the whole site (enforced by Caddy).
# BASIC_AUTH_HASH is a bcrypt hash with every $ doubled to $$ (compose escaping).
# Easiest: run ./set-password.sh — prompts silently and fills this in for you.
BASIC_AUTH_USER=
BASIC_AUTH_HASH=
