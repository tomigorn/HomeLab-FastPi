COMPOSE_PROJECT_NAME=reactive-resume

POSTGRES_USER=reactive
POSTGRES_PASSWORD=TODO-CHANGE-ME # Randomly generated in terminal: openssl rand -base64 24
POSTGRES_DB=reactive

# App runtime (placeholder values; change for production)
PUBLIC_URL=http://localhost:3000

# Secrets (use long random strings in real deployments)
ACCESS_TOKEN_SECRET=TODO-CHANGE-ME # Randomly generated in terminal: openssl rand -hex 32
REFRESH_TOKEN_SECRET=TODO-CHANGE-ME # Randomly generated in terminal: openssl rand -hex 32

# Chrome / PDF rendering
CHROME_TOKEN=TODO-CHANGE-ME # Randomly generated in terminal: openssl rand -hex 32
CHROME_URL=http://browserless:3000

# Storage (S3 / MinIO example)
STORAGE_URL=http://minio:9000
STORAGE_ENDPOINT=minio
STORAGE_PORT=9000
STORAGE_BUCKET=reactive-resume
STORAGE_ACCESS_KEY=minioadmin
STORAGE_SECRET_KEY=minioadmin