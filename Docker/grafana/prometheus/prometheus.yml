global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "raspberry-pi"
    static_configs:
      - targets:
          - node-exporter:9100

  # Traefik metrics (per-router/service request counts). Traefik is in a
  # different compose project/network, so scrape it via the host-published
  # internal entrypoint (:8080, LAN-only).
  - job_name: "traefik"
    static_configs:
      - targets:
          - "192.168.1.2:8080"
